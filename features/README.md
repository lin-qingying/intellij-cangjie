# Features

This directory is reserved for future product-level feature plugins.

The first stage still ships as a single plugin, but the new build graph already isolates the capability modules that future feature plugins will compose.
