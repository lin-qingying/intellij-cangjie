# Platform

This directory is reserved for IntelliJ Platform compatibility layers.

The first stage targets platform `253` only. Future version-specific source sets and adapters should live here instead of being spread across business modules.
