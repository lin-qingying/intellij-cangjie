# 错误代码 E491

**错误信息**：包名应为 'bson'

---

## 描述

编译器检测到当前包的名称与预期不符，期望包名为 'bson'，但实际包名不同。

## 触发场景
- 源文件所在目录或 package 声明的包名不是 'bson'。
- 例如：
  ```cangjie
  package mybson
  ```

## 解决方法
- 将 package 声明修改为 'bson'，确保包名与预期一致。
- 例如：
  ```cangjie
  package bson
  ```

---

[返回首页](./README.md)