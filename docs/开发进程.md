    
    
    
    
* 枚举声明具有枚举参数时，枚举值是否直接继承了枚举声明中的类型参数
* 当分析Option&lt;Int&gt;.None&lt;String&gt; 时，编译器前端是如何分析的，是对表达式进行整体分析，还是按顺序向下分析
* LSPMacroServer是由LSPServer启动的吗
* 我在启动LSPServer分析具有宏的代码时LSPMacroServer启动后又停止，是怎么回事，需要配置什么参数吗
  使用 <kbd>Ctrl</kbd>+<kbd>Alt</kbd>+<kbd>Del</kbd> 重启电脑


$$
\begin{Bmatrix}
a & b \\
c & d
\end{Bmatrix}
$$
