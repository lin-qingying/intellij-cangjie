?.  ??  

from import 的{}
运算符重载的()  //整个运算符重载都有问题
注解与宏 @
整形和浮点数的后缀  0u8  
[] 的 切片操作  [..1]

方法参数的  isNone!: Bool = false 形式 目前支持的为：isNone!: Bool 
lambda表达式

catch语句的简单无类型捕获  catch (_)  现在需要写成  catch (err: Error)

where子句
synchronized语句

try语句的作用域变量  try()
|> 运算符
type关键字 及其语法
