?.  ??  

from import 的{} 已完成
运算符重载的()  //整个运算符重载都有问题
注解与宏 @   已完成 但是宏token并没有解析，等什么时候做引用了在解析
整形和浮点数的后缀  0u8  
[] 的 切片操作  [..1]  已完成

方法参数的  isNone!: Bool = false 形式 目前支持的为：isNone!: Bool 已完成
lambda表达式

catch语句的简单无类型捕获  catch (_)  现在需要写成  catch (err: Error)

where子句
synchronized语句

try语句的作用域变量  try()
|> 运算符
type关键字 及其语法


unsafe 语句块 已完成

foreign 语句块 已完成


enum extend 中意外的构造函数   

class 中的析构函数  ~init  已完成


浮点数与整数混淆  已完成


type语句 已完成

区间类型 已完成