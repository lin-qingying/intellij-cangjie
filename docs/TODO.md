// 在前表示已修复但无法保证稳定性
// 在后表示不确定的bug 延后处理
没有 // 表示未修复

初始化语句中不能使用赋值表达式 但是可以用括号包裹

导入模块名验证与完全限定语法不能使用模块名
 
包导入自身
循环导入还存在问题

[//]: # (扩展中重写的成员不能使用override和redef)

[//]: # (多导入语句还没有实现)

静态上下文检查还需要优化  STATIC_CONTEXT_REFERENCE_ERROR
静态声明无法查找用法

[//]: # (方法中 当变量声明是最后一条语句时有问题)

[//]: # (悬浮文档中  where语句应该修正，现在 显示为  <T:Name>)

[//]: # (CALL 引用 不能识别)

当导入扩展的方法时，尽管爆红，但仍可以使用，不符合预期

[//]: # (静态方法的重写要使用redef覆盖而不能使用override)


[//]: # (非静态成员“aa”不能与父类或接口中的静态成员同名)

[//]: # (静态成员“aa”不能与父类或接口中的非静态成员同名)

[//]: # (总结，子类的静态成员不能与父类或接口的非静态成员同名，反之相同)
扩展的泛型必须用于被扩展类型

重导出语句在设计上不符合预期

枚举项没有补全  应尽快修复

// 在导入枚举类的情况下无法使用枚举项
插件感觉明显卡顿  //

cjpm包管理工具 
   在有库的情况下，在toml文件中删除库无法删除

lsp 
   lsp目前没有发送依赖的库路径
